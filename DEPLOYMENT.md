# Инструкция по деплою IdenSelf с Bridge API

## Часть 1: Развертывание Bridge API на Render.com

### Шаг 1: Создание аккаунта на Render.com
1. Перейдите на https://render.com
2. Зарегистрируйтесь или войдите через GitHub

### Шаг 2: Подключение GitHub репозитория
1. В панели Render нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий `zagovormasonov/idenself`
3. Дайте разрешение Render на доступ к репозиторию

### Шаг 3: Настройка Web Service
Заполните следующие поля:

- **Name**: `idenself-bridge` (или любое другое имя)
- **Region**: `Frankfurt (EU Central)` (ближайший к России регион)
- **Branch**: `main`
- **Root Directory**: `bridge`
- **Runtime**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`
- **Instance Type**: `Free` (для начала)

### Шаг 4: Добавление переменных окружения
В разделе **Environment Variables** добавьте:

| Key | Value |
|-----|-------|
| `GEMINI_API_KEY` | Ваш API ключ от Google Gemini AI |
| `DATABASE_URL` | Строка подключения к PostgreSQL (например: `postgresql://user:password@host:5432/dbname`) |

> **Где взять GEMINI_API_KEY:**
> 1. Перейдите на https://makersuite.google.com/app/apikey
> 2. Войдите в Google аккаунт
> 3. Нажмите "Create API key"
> 4. Скопируйте ключ

### Шаг 5: Деплой
1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (обычно 2-3 минуты)
3. После успешного деплоя скопируйте URL вашего сервиса
   - Он будет выглядеть как: `https://idenself-bridge-xxxx.onrender.com`

### Шаг 6: Проверка работоспособности
Откройте в браузере:
```
https://your-bridge-url.onrender.com/health
```

Вы должны увидеть JSON ответ:
```json
{
  "status": "ok",
  "timestamp": "2025-12-19T..."
}
```

---

## Часть 1.5: Развертывание NestJS сервера на Render.com

### Шаг 1: Создание второго Web Service для NestJS сервера
1. В панели Render нажмите **"New +"** → **"Web Service"**
2. Выберите ваш GitHub репозиторий `zagovormasonov/idenself`
3. Дайте разрешение Render на доступ к репозиторию

### Шаг 2: Настройка Web Service
Заполните следующие поля:

- **Name**: `idenself-server` (или любое другое имя)
- **Region**: `Frankfurt (EU Central)`
- **Branch**: `main`
- **Root Directory**: `server`
- **Runtime**: `Node`
- **Build Command**: `npm install && npx prisma generate`
- **Start Command**: `npm run start:prod`
- **Instance Type**: `Free`

### Шаг 3: Добавление переменных окружения
В разделе **Environment Variables** добавьте:

| Key | Value |
|-----|-------|
| `DATABASE_URL` | Ваша PostgreSQL строка подключения |
| `JWT_SECRET` | Случайная строка для JWT токенов |
| `BRIDGE_API_URL` | URL вашего Bridge API (например: `https://idenself-bridge-xxxx.onrender.com`) |
| `PORT` | `3000` |

### Шаг 4: Деплой NestJS сервера
1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя
3. Скопируйте URL NestJS сервера

### Шаг 5: Обновление Bridge API
Добавьте переменную окружения в Bridge API:
- **NESTJS_API_URL**: URL вашего NestJS сервера (например: `https://idenself-server-xxxx.onrender.com`)

---

## Часть 2: Настройка основного сервера

### Шаг 1: Подключение к серверу
```bash
ssh root@5.129.250.81
cd ~/idenself
```

### Шаг 2: Создание файла с переменными окружения
```bash
nano .env
```

Добавьте в файл:
```env
BRIDGE_API_URL=https://your-bridge-url.onrender.com
NESTJS_API_URL=https://your-server-url.onrender.com
```

**Важно:** Замените `https://your-bridge-url.onrender.com` на реальный URL вашего Bridge API из Render.com!

Сохраните файл (`Ctrl+X`, затем `Y`, затем `Enter`)

### Шаг 3: Обновление docker-compose для использования .env файла
```bash
nano docker-compose.yml
```

Убедитесь, что в секции `app-server` → `environment` есть строка:
```yaml
- BRIDGE_API_URL=${BRIDGE_API_URL:-}
```

### Шаг 4: Загрузка последних изменений из GitHub
```bash
git pull origin main
```

### Шаг 5: Пересборка и перезапуск контейнеров
```bash
sudo docker compose down
sudo docker compose up -d --build
```

### Шаг 6: Проверка работы
Откройте браузер и перейдите на ваш сайт. Попробуйте:
1. Зарегистрироваться
2. Описать жалобу
3. Пройти опросник

Если все работает - вопросы будут генерироваться через Gemini AI! 🎉

---

## Проверка логов

### Логи Bridge API (на Render.com):
В панели Render перейдите в ваш Web Service → вкладка **Logs**

### Логи основного сервера:
```bash
# Все сервисы
sudo docker compose logs

# Только сервер
sudo docker compose logs app-server

# С отслеживанием в реальном времени
sudo docker compose logs -f app-server
```

---

## Устранение проблем

### Bridge API не отвечает
1. Проверьте, что сервис запущен в панели Render
2. Проверьте логи в Render
3. Убедитесь, что `GEMINI_API_KEY` установлен корректно

### Основной сервер не может подключиться к Bridge
1. Проверьте, что `BRIDGE_API_URL` установлен в `.env` файле
2. Проверьте формат URL (должен начинаться с `https://` и **не** иметь `/` в конце)
3. Проверьте логи: `sudo docker compose logs app-server`

### Приложение использует моковые данные
- Это нормальное поведение, если Bridge API недоступен
- Проверьте логи сервера, там будет сообщение: "Bridge API URL not configured, using mock data"

---

## Важные замечания

### Бесплатный тариф Render.com
- Сервис "засыпает" после 15 минут неактивности
- Первый запрос после "сна" может занять 30-60 секунд
- Для production рекомендуется платный план ($7/месяц)

### Безопасность
- API ключ Gemini хранится **только** на Render.com
- Основной сервер в РФ не имеет прямого доступа к Google API
- Все запросы идут через bridge API

### Лимиты Gemini API
- Бесплатный тариф: 60 запросов в минуту
- Если лимит превышен, пользователи увидят моковые данные
- Для production рекомендуется настроить квоты

---

## Обновление кода

Когда вы вносите изменения в код:

```bash
# На вашем локальном компьютере
git add .
git commit -m "Описание изменений"
git push origin main

# На сервере
ssh root@5.129.250.81
cd ~/idenself
git pull origin main
sudo docker compose up -d --build
```

Bridge API на Render.com обновится автоматически при push в main ветку (если настроена автоматическая деплой-интеграция).

---

## Контакты для поддержки

Если возникли вопросы:
1. Проверьте логи сервисов
2. Убедитесь, что все переменные окружения установлены
3. Проверьте статус сервисов на Render.com


