# Подключение к Gemini API через idenself.com

## Быстрая настройка

Ваш сервер уже готов использовать Gemini API через https://idenself.com!

### На сервере РФ:

```bash
# Подключитесь к серверу
ssh root@5.129.250.81
cd ~/idenself

# Создайте или отредактируйте файл .env
nano .env
```

Добавьте в `.env`:
```env
BRIDGE_API_URL=https://idenself.com
```

Сохраните (`Ctrl+X`, затем `Y`, затем `Enter`)

```bash
# Обновите код из GitHub
git pull origin main

# Перезапустите контейнеры
sudo docker compose down
sudo docker compose up -d --build
```

## Готово! 🎉

Теперь ваше приложение будет использовать Gemini AI через https://idenself.com для генерации:
- Персонализированных вопросов на основе жалоб пользователя
- Углубленных вопросов на основе первых ответов
- Детальных планов и рекомендаций

## Проверка работы

После перезапуска контейнеров:

1. Откройте ваш сайт (http://5.129.250.81 или http://idenself.ru)
2. Зарегистрируйтесь или войдите
3. Опишите какую-нибудь жалобу
4. Вопросы теперь будут генерироваться AI на основе вашего описания!

## Логи

Проверить работу можно через логи:

```bash
# Все логи сервера
sudo docker compose logs -f app-server

# Поиск сообщений о Bridge API
sudo docker compose logs app-server | grep -i bridge
```

## Ожидаемые эндпоинты на idenself.com

Приложение ожидает следующие эндпоинты:

### 1. POST /api/generate-part1
**Запрос:**
```json
{
  "complaint": "Описание жалобы пользователя"
}
```

**Ответ:**
```json
{
  "questions": [
    {
      "id": "q1",
      "text": "Текст вопроса",
      "type": "text",
      "options": []
    },
    {
      "id": "q2",
      "text": "Текст вопроса",
      "type": "choice",
      "options": ["вариант 1", "вариант 2"]
    },
    {
      "id": "q3",
      "text": "Текст вопроса",
      "type": "scale",
      "options": []
    }
  ]
}
```

### 2. POST /api/generate-part2
**Запрос:**
```json
{
  "complaint": "Исходная жалоба",
  "answerspart1": {
    "q1": "ответ на первый вопрос",
    "q2": "выбранный вариант",
    "q3": 7
  }
}
```

**Ответ:** такой же формат с `questions[]`

### 3. POST /api/generate-results
**Запрос:**
```json
{
  "complaint": "Исходная жалоба",
  "answerspart1": { ... },
  "answerspart2": { ... }
}
```

**Ответ:**
```json
{
  "personalPlan": "Подробный персональный план...",
  "psychPrep": "Подготовка к сеансам...",
  "specialistDoc": "Документ для специалистов...",
  "recommendedTests": [
    {
      "name": "Название теста",
      "description": "Описание"
    }
  ]
}
```

## Fallback на mock-данные

Если API недоступен или возвращает ошибку, приложение автоматически использует заранее подготовленные mock-данные, чтобы пользователи могли тестировать функционал.

## Важно

- Убедитесь, что на https://idenself.com настроены CORS для приема запросов от вашего сервера
- Timeout для генерации: 60 секунд для вопросов, 120 секунд для результатов
- Все запросы идут с заголовком `Content-Type: application/json`

